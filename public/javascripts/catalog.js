// Giữ nguyên query khi chuyển trang bằng form GET (đã có)
(function(){
  const f = document.getElementById('catFilter');
  if (!f) return;
  // Có thể thêm debounce, validate pmin/pmax, v.v. – hiện tại để submit bình thường
})();